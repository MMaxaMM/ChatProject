syntax = "proto3";

package audio;

option go_package = "MMaxaMM.audio.v1;audiov1";

service AudioService {
    rpc Recognize (AudioRequest) returns (AudioResponse);
}

message AudioRequest {
    bytes audio = 1; // Аудиофайл в бинарном формате
}

message AudioResponse {
    string result = 1;
    string error = 2;
}